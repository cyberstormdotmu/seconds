create index `user_id_idx` on account (userId);

create index `wallet_id_idx` on account (walletId);

create index `account_attr_key_idx` on accountattribute (akey);

create index `transaction_attr_key_idx` on transactionattribute (akey);