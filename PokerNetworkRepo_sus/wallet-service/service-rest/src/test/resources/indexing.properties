# Elasticsearch cluster name
# elastic.cluster.name=elasticsearch

# Url for elastic search client, the path is the index name. Empty to DISABLE indexing.
# elastic.client.url=http://localhost:9300/network

